requests==2.31.0
pandas>=2.0.0
numpy<=1.26.4
# --- Library สำหรับดึงข้อมูลราคา ---
requests==2.31.0
beautifulsoup4==4.12.2

# --- Library สำหรับจัดการข้อมูลและตัวเลข ---
pandas==2.1.1
numpy==1.26.0

# --- (ถ้ามี) สำหรับจัดการเวลาหรือไฟล์พิเศษ ---
python-dateutil==2.8.2
